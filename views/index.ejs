<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="#044aab"/>
    <title>Revision Access Point</title>
    <link rel="icon" type="image/x-icon" href="/logo.svg">
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/loginLoad.css">
    <link rel="manifest" href="/manifest.json">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&display=swap" rel="stylesheet">
    <!-- Sweet alert -->
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
    <!-- Ad sense -->
    <!-- <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4066811607299460" crossorigin="anonymous"></script> -->
  </head>
  <body>
        <div class="d-flex justify-content-center align-items-center" style="height: 100vh;">

            <div class="p-0 d-flex justify-content-center align-items-center" style="width: 80vw;" >
                <div class="card text-muted" style="width: 100%;">
                    <div class="card-body m-0">
                        <div class=" mb-4 d-flex align-items-center justify-content-center" id="logo" style="margin-top: 10px;">
                            <h4 class="h4 text-dark mb-0" style="font-weight: bold;">Revision Access Point</h4>
                        </div>

                        <form action="/" method="post" class="" id="loginForm">
                            <div class="mb-3 p-2 rounded d-flex justify-content-start align-items-center" style="background-color: #f2f2f2;">
                                <i class="fa-solid fa-envelope text-muted me-2 ms-2"></i>
                                <input type="text" class="flex-fill" id="email" name="email" placeholder="Email" style="border: none; background-color: #f2f2f2; outline: none; font-weight: 300;" required>
                            </div>
    
                            <div class="mb-3 p-2 rounded d-flex justify-content-start align-items-center" style="background-color: #f2f2f2;">
                                <i class="fa-solid fa-lock text-muted me-2 ms-2"></i>
                                <input type="password" class="flex-fill" id="password" name="password" placeholder="Password" style="border: none; background-color: #f2f2f2; outline: none; font-weight: 300;" required>
                                <i class="fa-solid fa-eye text-muted me-2 ms-2" id="seePass" onclick="seePass()"></i>
                            </div>
                            
                            <button type="submit" class="btn bg-dark" style="width: 100%;" id="loginBtn" onclick="login">
                                <h6 class="h6 text-light" style="font-size: 20px;">
                                    Login 
                                </h6>
                                <div class="loadDiv container d-flex flex-column align-items-center justify-content-center" >
                                    <div class="load bg-dark" style="display: none;">
                                        <div class="load2 bg-dark">
                                        </div>
                                    </div>
                                </div>
                            </button>

                            <div class="d-flex flex-column justify-content-center align-items-center">
                                <h6 class="h6 text-muted mt-3" style="font-weight: 300;">Back to <a class="text-muted" href="/signUp" id="signUpLink"><strong style="text-decoration: underline;">Sign up</strong></a></h6>
                                <h6 style="font-size: 12px; color: darkgray;">icons by <a href="https://icons8.com/" style="color: darkgray;">icons8</a></h6>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

        </div>
    <!-- </div> -->

    <!-- If the message is in the route query -->
    <% if (message) { %>
        <script>
            function display(){
                let display = "<%= message %>"
                swal(display);
            }

            // To ensure the alert is displayed only once
            // Once the browser is closed
            if (!sessionStorage.getItem('firstLoadDone')) {
                window.onload = display()
                sessionStorage.setItem('firstLoadDone', 'true');
            }

            // Clearing the session storage when the redirect happens
            let loginForm = document.getElementById("loginForm")
            let signUpLink = document.getElementById("signUpLink")

            loginForm.onsubmit = function(){
                if (sessionStorage.getItem('firstLoadDone')) {
                    sessionStorage.removeItem('firstLoadDone');
                }
            }

            signUpLink.onclick = function(){
                if (sessionStorage.getItem('firstLoadDone')) {
                    sessionStorage.removeItem('firstLoadDone');
                }
            }

            
            </script>
    <% } %>
    <script src="/js/index.js"></script>
    <script src="https://kit.fontawesome.com/69a11c0c5f.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script>
        if('serviceWorker' in navigator) {
            navigator.serviceWorker.register('/service-worker.js').then(function(registration){
                console.log('Service Worker registered');
            }).catch(function(error){
                console.log('Service Worker registration failed:', error);
            })
        }
    </script>
  </body>
</html>